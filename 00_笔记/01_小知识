
------------>    pip install 模块名 -i http://pypi.douban.com/simple/ --trusted-host pypi.douban.com    -->(国内地址 )

1.在pycharm中可以利用# TODO 来标记待完成的工作内容   可以在View中查看任务
2.在python中:可以通过id()函数来查看引用数据类型的内存地址
3.哈希是一种算法，其作用就是提取数据的特征码（指纹）----->  hash(#)  参数化只能为不可变类型
        - 相同的内容 得到 相同的结果
        - 不同的内容 得到 不同的结果
4.- 局部变量 是在 函数内部 定义的变量，只能在函数内部使用
 - 全局变量 是在 函数外部定义的变量（没有定义在某一个函数内），所有函数 内部 都可以使用这个变量
> 提示：在其他的开发语言中，大多 不推荐使用全局变量 —— 可变范围太大，导致程序不好维护！

5.如果不知道设置什么初始值，可以设置为None

6.  is 是判断两个标识符是不是引用同一个对象 	      x is y，    类似 id(x) == id(y)
    is not 是判断两个标识符是不是引用不同对象 	  x is not y，类似 id(a) != id(b)

    is 与 == 区别：
        is 用于判断两个变量引用对象是否为同一个
        == 用于判断引用变量的值 是否相等

        >>> a = [1, 2, 3]
        >>> b = [1, 2, 3]
        >>> b is a
        False
        >>> b == a
        True
7.super().父类方法  ---->  一定要有()

8.静态方法可以被继承类使用

9.在python2.7的版本中支持中文就要添加一行
    # -*- coding: UTF-8 -*-

9.文件路径设置为 . 表示当前目录  (path)  直接写代表从当前目录开始,也支持绝对路径
    file_read = open("README[复件1]//README")  当前目录README[复件1]文件夹下的README文件
    os.listdir("C://Users//Administrator//Desktop")  桌面文件夹

10.在继承方法时,如果父类不是object类,则需要在__init方法中重写父类方法    ---->  super().__init__()